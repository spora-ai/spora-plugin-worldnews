sonar.projectKey=spora-ai_spora-plugin-worldnews
sonar.organization=spora-ai

# Sources and tests
sonar.sources=src
sonar.tests=tests
sonar.test.inclusions=**/*Test.php,**/*.Test.php
sonar.exclusions=vendor/**,storage/**

# PHP test coverage (clover XML produced by `pest --coverage-clover=coverage.xml`)
sonar.php.coverage.reportPaths=coverage.xml

# 30-day leak period. SonarCloud's "Sonar way" quality gate evaluates `new_*`
# metrics — without a leak period, the gate is never computed and the project
# overview shows "no data". Mirrors the project-level setting on spora-core.
sonar.leak.period=30
